[distutils]
index-servers = pypi-local

[pypi-local]
repository: https://otomato.jfrog.io/artifactory/api/pypi/pypi-local
username: ${{ secrets.PYPI_USERNAME }}
password: ${{ secrets.PYPI_PASSWORD }}
